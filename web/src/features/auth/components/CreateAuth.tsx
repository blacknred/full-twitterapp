import type { FC } from "react";

export const CreateAuth: FC = () => null

// Form[email/username, password], SSO


// errors?.forEach(([name, message]) => {
//   methods.setError(name, { message })
// })